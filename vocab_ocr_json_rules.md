
# Vocabulary Image → JSON Conversion Rules

이 문서는 단어장 이미지에서 단어를 OCR로 추출하여 **JSON 단어장 파일로 변환할 때 반드시 따라야 하는 규칙**을 정리한 것이다.
다음 변환 작업에서도 동일한 기준으로 사용한다.

---

# 1. 출력 JSON 형식

반드시 아래와 같은 **배열(JSON Array) 형식**으로 출력한다.

```json
[
  {"no":1,"word":"example","pos":"noun","meaning":"예시"},
  {"no":2,"word":"another","pos":"verb","meaning":"다른"}
]
```

필드 설명

| 필드 | 설명 |
|-----|-----|
| no | 단어 번호 (이미지의 번호 그대로 사용) |
| word | 영어 단어 |
| pos | 품사 |
| meaning | 한국어 의미 |

---

# 2. 품사 변환 규칙

이미지에 표시된 품사를 다음과 같이 변환한다.

| 이미지 표시 | JSON pos |
|-------------|-----------|
명 | noun |
동 | verb |
형 | adjective |
부 | adverb |
전 | preposition |
접 | conjunction |

이미지에 품사가 없는 경우 **문맥 기반으로 추론하지 않는다.**

---

# 3. 단어 추출 규칙

## 3.1 번호 유지

이미지의 번호를 그대로 사용한다.

예

```
1 notorious
2 murderer
3 criminal
```

→

```json
{"no":1,"word":"notorious"}
```

번호는 **절대 재정렬하지 않는다.**

---

# 4. 의미(meaning) 추출 규칙

## 4.1 의미는 절대 수정하지 않는다

이미지에 있는 한국어 의미는 **그대로 복사한다.**

예

```
~에 관한
큰 감동을 주다
부족, 결핍
```

수정 금지

잘못된 예

```
감동시키다
~에 대한
```

---

## 4.2 "~" 표현 유지

다음 표현은 반드시 그대로 유지한다.

```
~에 관한
~인 척하다
~을 할 수 없는
~에게 동기를 주다
```

절대 변형하지 않는다.

---

## 4.3 괄호 의미 규칙

괄호 내부 쉼표는 의미 분리 대상이 아니다.

예

```
(장소, 시간이) 일치하는
```

→ 의미 **1개**

절대 다음처럼 분리하지 않는다.

```
(장소
시간이) 일치하는
```

---

## 4.4 쉼표 의미 규칙

쉼표가 있는 경우 **의미 그대로 유지한다.**

예

```
그저, 단순히
just, merely
```

JSON에서도 동일하게 유지한다.

---

# 5. OCR 판독 불가 처리

이미지에서 글자가 가려졌거나 읽을 수 없는 경우 다음 규칙을 따른다.

절대 하지 말 것

- 의미 추측
- 단어 임의 수정
- 품사 추측

대신 다음 형식으로 보고한다.

```
OCR_ERROR:
no: XX
reason: text not readable
```

---

# 6. 절대 금지 사항

다음 작업은 금지된다.

1. 의미 임의 수정
2. 단어 철자 수정
3. 번호 재정렬
4. 의미 번역
5. 의미 요약
6. 의미 분리
7. 의미 추가

모든 데이터는 **이미지 그대로 복사한다.**

---

# 7. 최종 검증 규칙

JSON 출력 전 반드시 확인

- 단어 개수 = 80
- 번호 1~80 존재
- word 공백 없음
- meaning 공백 없음
- JSON syntax 오류 없음

---

# 8. 목표 출력

최종 결과는 다음과 같은 **단어장 JSON 파일**이다.

```
vocabulary_01.json
```

구조

```
[
 {no, word, pos, meaning},
 {no, word, pos, meaning},
 ...
]
```

---

# End of Rules
