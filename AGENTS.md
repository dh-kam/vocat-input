# Vocat 단어 자동 입력 스크립트 - 작업 가이드

## 프로젝트 개요
ADB(Android Debug Bridge)와 UIAutomator를 통해 Vocat 앱에 영어 단어와 의미를 자동으로 입력하는 시스템

---

## 1. 데이터 구조 및 처리

### 입력 JSON 형식
```json
[
  {
    "no": 1,
    "word": "critical",
    "pos": "adjective",
    "meaning": "위독한"
  },
  {
    "no": 2,
    "word": "assign",
    "pos": "verb",
    "meaning": "맡기다, 배정하다"
  }
]
```

### 필드 설명
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `no` | number | ✅ | 번호 |
| `word` | string | ✅ | 영어 단어 또는 숙어 |
| `pos` | string | ✅ | 품사 (영어: adjective, noun, verb, adverb 등) |
| `meaning` | string \| string[] | ✅ | 한국어 의미 (쉼표로 구분된 여러 의미 가능) |

### 데이터 처리 규칙

#### 1) 의미(Meaning) 처리
- **입력 형식:** 쉼표(`,`)로 구분된 여러 의미
  - 예: `"의미1, 의미2, 의미3"`
- **처리 방식:**
  - 쉼표가 있으면 각각을 별도의 의미로 인식
  - 각 의미를 "한국어" EditText에 순차적으로 입력
  - 첫 번째 의미 입력 후 엔터 → 다음 의미 입력
- **자동 정규화:** 각 의미의 앞뒤 공백 제거

#### 2) 품사(POS) 변환
영어 품사 → 한글 축약어 (한 글자)

| 영어 | 축약어 |
|------|--------|
| adjective | 형 |
| noun | 명 |
| verb | 동 |
| adverb | 부 |
| preposition | 전 |
| conjunction | 접 |
| article | 관 |
| interjection | 감 |

---

## 2. UI 자동화 프로세스

### 화면 상태 확인

#### 2.1 단어장 화면 (Wordbook Screen)
**판단 기준:**
- `content-desc="단어 추가"` 요소 존재 (우측 상단 + 버튼)

**함수:** `is_wordbook_screen()`

#### 2.2 단어 입력 화면 (Word Input Screen)
**판단 기준:**
- `hint="영어"` 또는 `content-desc="영어"` 존재
- `hint="한국어"` 존재
- `hint="품사"` 존재
- **추가 확인:** 영어/한국어 EditText가 비어있는 상태 (새 입력 준비 상태)

**함수:** `is_word_input_screen()`

#### 2.3 중복 단어 대화상자
**판단 기준:**
- `content-desc="중복 단어"` 존재

**함수:** `is_duplicate_word_dialog()`

### 입력 프로세스

#### Phase 1: 초기화
```
1. 단어장 화면 확인
2. + 버튼(단어 추가) 클릭
3. 단어 입력 화면 도달 대기 (최대 10초)
```

#### Phase 2: 단어 입력
```
1. 영어 EditText에 포커스 설정
2. 1초 대기 (포커스 안정화)
3. adb shell input text로 영어 단어 입력
4. 엔터 키 입력 (다음 필드로 포커스 이동)
5. 2초 대기 (한국어 EditText 포커스 안정화)
```

#### Phase 3: 의미 입력
```
1. 의미를 쉼표로 구분하여 배열 생성
2. 각 의미를 순차적으로 입력:
   - 첫 번째 의미: 직접 입력
   - 나머지 의미: 엔터 후 입력 반복
3. 2초 대기 (품사 EditText 포커스 이동)
```

#### Phase 4: 품사 입력
```
1. 품사 EditText에 포커스 설정
2. 영어 품사를 한글 축약어로 변환하여 입력
3. 2초 대기
```

#### Phase 5: 저장 및 중복 처리
```
1. 추가(저장) 버튼 클릭
2. 중복 단어 대화상자 확인 (최대 3초)
   - 있으면: "신규 단어로 추가" 버튼 클릭
   - 없으면: 자동 저장됨
3. 단어 입력 화면 복귀 대기 (최대 10초)
4. 영어/한국어 EditText 빈 상태 확인 (새 입력 준비 확인)
```

---

## 3. 입력 방식

### 영어 단어 입력
**방식:** `adb shell input text "<단어>"`
- 특수 문자는 escape 처리 필요
- 엔터 키로 다음 필드 이동

### 한글 의미 입력
**방식:** 
1. 각 의미를 EditText에 직접 입력
2. 엔터 키로 다음 의미 추가
3. 기존 방식: 클립보드 + 붙여넣기 (레거시, 필요시 사용)

### 포커스 이동 간 딜레이
- **기본 딜레이:** 2초
- **첫 포커스 설정:** 1초 (추가 안정화)
- **저장 후 화면 전환:** 1초

---

## 4. 에러 처리 및 안전장치

### 필수 검증 (Validation)

#### 4.1 JSON 파일 검증
```bash
1. 파일 존재 여부 확인
2. JSON 형식 유효성 확인
3. 각 필드 필수 여부 확인:
   - no, word, pos, meaning 모두 필수
4. 빈 배열 체크
5. 데이터 타입 확인:
   - meaning: string 또는 string[] 모두 가능
```

#### 4.2 런타임 화면 검증
**원칙:** "정확한 화면이 아니면 입력하지 않기"

```bash
1. 각 입력 전 화면 상태 확인
2. 예상 화면이 아니면:
   - 명확한 에러 메시지 출력
   - 현재 화면 상태 정보 제공
   - 프로그램 즉시 종료
3. 최대 대기 시간 초과 시 동일 처리
```

### 에러 메시지 형식
```
ERROR: [상황]
현재 상태: [현재 화면]
예상 상태: [예상 화면]
조치: [해결 방법]
```

예시:
```
ERROR: 단어 입력 화면을 찾을 수 없습니다
현재 상태: 불명확한 화면
예상 상태: 영어/한국어/품사 EditText가 있는 입력 화면
조치: 앱에서 '단어 추가' 버튼을 눌러 올바른 화면으로 이동 후 재시도
```

---

## 5. 스크립트 실행

### 사용법
```bash
./vocat_input.sh <JSON_파일_경로>
```

### 예시
```bash
./vocat_input.sh 20260317_단어시험.json
./vocat_input.sh /path/to/words.json
```

### 옵션
- JSON 파일은 필수 인자
- 상대 경로 또는 절대 경로 모두 가능

### 반환값
- `0`: 모든 단어 입력 성공
- `1`: 입력 중 오류 발생 (화면 감지 실패, JSON 검증 실패 등)

---

## 6. 디버깅 및 테스트

### 로그 출력
- 모든 작업에 타임스탬프 포함
- 형식: `[HH:MM:SS] 메시지`
- 레벨: ERROR, WARNING, INFO, DEBUG

### UI 덤프 확인
```bash
adb shell uiautomator dump /sdcard/window_dump.xml
adb shell cat /sdcard/window_dump.xml
```

### 단어 입력 테스트
- 작은 규모 JSON으로 먼저 테스트 (예: 5개 단어)
- 각 단계별 로그 확인
- 실패 지점 정확히 파악

### tmux 세션 관리
```bash
# 세션 생성
tmux new-session -d -s vocat-output

# 세션 접속
tmux attach-session -t vocat-output

# 명령 실행
tmux send-keys -t vocat-output "command" Enter

# 세션 종료
tmux kill-session -t vocat-output
```

---

## 7. 주의사항

### 중요
1. ⚠️ **정확한 화면 확인 후에만 입력**: 잘못된 화면에 입력 금지
2. ⚠️ **포커스 이동 딜레이 준수**: 최소 2초 이상 필요
3. ⚠️ **중복 단어 신규 추가**: 항상 "신규 단어로 추가" 선택
4. ⚠️ **JSON 검증 필수**: 데이터 무결성 확인 후 실행

### 포장팩터(Screen Size) 변화
- **좌표 하드코딩 금지**: 모든 위치를 UIAutomator bounds에서 동적 추출
- **bounds 형식:** `[x1,y1][x2,y2]` → 중심점 계산: `x=(x1+x2)/2, y=(y1+y2)/2`

### 성능
- 단어당 약 30~40초 소요 (포커스 이동 및 입력 포함)
- 대량 입력 시 배터리 및 발열 주의

---

## 8. 참고: 기술 스택

- **언어:** Bash
- **도구:** ADB, UIAutomator
- **의존성:** jq 또는 python3 (JSON 파싱)
- **타겟 앱:** Vocat (kr.co.devstory.vocat)

---

## 9. 향후 개선 사항

- [ ] 병렬 처리 (여러 단어 동시 입력)
- [ ] 웹 UI 제공
- [ ] 실시간 모니터링 대시보드
- [ ] 입력 실패 재시도 메커니즘
- [ ] 로그 파일 자동 저장
- [ ] 입력 이력 추적
